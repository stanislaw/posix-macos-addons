#include "posix-mac-pthread.h"

#include <gtest/gtest.h>

#include <iostream>

TEST(Pthread_SetSchedPrio_test, 01) {
  // TODO: placeholder just to make stuff compiled. Write something real later.
  while (0) {
    pthread_setschedprio(pthread_self(), 0);
  }
}
